### Not working in windows
# guard 'jekyll-plus', :serve => true do
#   watch /.*/
#   ignore /^_site/
# end

guard 'rake', :task => 'my-jekyll' do
  # watch(%r{^some_files/.+$})
  watch /.*/
  ignore /^_site/
end
